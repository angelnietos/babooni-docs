<p align="center">
  <img src="../../../assets/arquetipos-mark.svg" width="56" alt="Arquetipos" />
</p>

<h1 align="center">F64-B1 — Mobile e2e + parity manifest</h1>

<p align="center">
  <img alt="arquetipos" src="https://img.shields.io/badge/arquetipos-0f766e?style=flat-square" />
  <a href="../../../README.md"><img alt="Biblia" src="https://img.shields.io/badge/hub-biblia-0f766e?style=flat-square" /></a>
</p>

## Estado

listo para ejecutar

## Objetivo

Cerrar restos [F63-C1](../plans-63-sixty-three-round/1750000209000-f63-mobile-feature-shell-parity.md):
nav/shell hechos; faltan e2e `/clients` y rutas mobile en parity.

## Entregables

1. Ampliar `ionic-*-e2e` smoke: login demo → `/clients` (tabla o lista visible).
2. Opcional soft: `/settings` + assert `data-arq-tenant` tras switcher.
3. Actualizar `docs/arquetipos/parity.manifest.yaml` (+ `parity.md`) con rutas
   Ionic/RN (`clients`, `users`, `roles`, `audit`, `settings`, `native-ui`).
4. Script root / CI soft: `pnpm arq:mobile:e2e:smoke` (o target Nx existente).

## Criterios de aceptación

- [ ] `ionic-multi-e2e` (chromium) login→clients verde en local / CI soft.
- [ ] `pnpm check:arquetipos-parity` conoce rutas mobile (no silent drift).

## Verificación

```bash
pnpm nx run ionic-multi-e2e:e2e -- --project=chromium
pnpm check:arquetipos-parity -- --strict
```

## Depende de

—

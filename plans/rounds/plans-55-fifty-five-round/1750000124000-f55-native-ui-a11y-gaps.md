# F55-B3 — A11y gaps native-ui (modal focus trap + tabs teclado)

## Estado

listo para ejecutar

## Objetivo

F54-B3 dejó pass en button/input/modal roles; **defer**: focus trap en modal y
navegación por flechas en tabs/segment.

## Tareas

1. Focus trap + restore focus en `base-modal` al abrir/cerrar.
2. Arrow Left/Right (o Home/End) en `base-tabs` / `base-segment`.
3. Extender `native-ui-a11y.spec.ts` / matriz.
4. Known gaps restantes → F56 sin ocultarlos.

## Criterios de aceptación

- [ ] Modal trap pass (test o checklist manual documentada).
- [ ] Tabs o segment: al menos un CE con teclado pass.
- [ ] Matriz a11y actualizada.

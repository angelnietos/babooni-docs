<p align="center">
  <img src="../../../assets/arquetipos-mark.svg" width="56" alt="Arquetipos" />
</p>

<h1 align="center">F62-A4 — React Native: paridad de tokens (solo tokens)</h1>

<p align="center">
  <img alt="arquetipos" src="https://img.shields.io/badge/arquetipos-0f766e?style=flat-square" />
  <a href="../../../README.md"><img alt="Biblia" src="https://img.shields.io/badge/hub-biblia-0f766e?style=flat-square" /></a>
</p>

## Estado

listo para ejecutar

## Objetivo

RN **no** monta Lit. La paridad visual es por **`@base/ui-tokens` / `./rn`**:
mismos nombres semánticos (brand, surface, motion, type) que el DOM, aplicados
en `@base/react-native-ui` y apps `react-native-*` arquetipos.

Carry / cierre de la nota F60 “RN espejo tokens/API, no Lit”.

## Entregables

1. Auditoría: `ui-tokens/tokens.ts` ↔ `ui-tokens/rn.ts` (o path canónico) —
   sin huecos en brand/CTA/hero/status/motion añadidos en F60/F62-A1.
2. Aplicar tokens en primitivos RN usados por login/clients demo (colores,
   radios, tipografía) — **sin** portar composiciones Lit.
3. Doc corta en `parity.md` / design-system: “RN = Token level”; checklist
   visual Expo web opcional.
4. Si falta espejo: añadir keys + test de paridad de nombres (soft).

## Criterios de aceptación

- [ ] Cada token semántico nuevo DOM tiene equivalente RN documentado.
- [ ] Apps RN arquetipos no hardcodean indigo/coral legacy.
- [ ] `pnpm nx typecheck` proyectos RN tokens/ui relevantes verdes.

## Verificación

```bash
pnpm nx typecheck base-ui-tokens
# + typecheck base-react-native-ui / arquetipos RN si aplica
rg -n "#4f46e5|#6366f1" apps/arquetipos/mobile libs/base/frontend/mobile/react-native --glob '*.{ts,tsx}' || true
```

## Depende de

F62-A1 (matriz temas → qué espejar).

## Bloquea

Nada DOM; cierra criterio “resto F60 RN”.

## Fuera de alcance

Lit en RN; reescribir pantallas RN a BEM web.

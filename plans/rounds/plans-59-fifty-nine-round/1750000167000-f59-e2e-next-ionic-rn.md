# F59-C1 — E2E / smoke Next · Ionic · RN

## Estado

completado

## Objetivo

Cerrar el gap de F58-D2: stacks **sin** Playwright canónico
(`next-single` / `next-multi`, Ionic, React Native) tienen al menos checklist
manual + comando smoke de **build/serve** documentado, o un e2e mínimo si el
coste es bajo.

## Entregables

1. Tabla en [parity.md](../../../arquetipos/parity.md): qué existe / qué falta.
2. Scripts o docs: `pnpm arq:fe:next-single` smoke build; Ionic/RN build smoke
   si ya hay targets Nx.
3. Opcional: un spec Playwright contra Next solo login redirect (soft CI).
4. Si no hay presupuesto: defer F60 con motivacion (no silencio).

## Hecho

- Playwright smoke: `next-*-e2e`, `ionic-*-e2e`, `react-native-*-e2e` (Expo web).
- Scripts: `pnpm arq:fe:e2e:next`, `pnpm arq:mobile:e2e:smoke`, `pnpm arq:e2e:all`,
  `pnpm arq:typecheck:mobile-next`; `arq:fe:build:smoke` incluye next+ionic.
- Auth demo local (no Keycloak) — documentado en parity.md.

## Criterios de aceptación

- [x] Parity.md ya no deja Next/Ionic/RN como “…” sin owner.
- [x] Al menos build smoke documentado y ejecutable localmente.
- [x] No romper `arq:fe:e2e:smoke` (SPA) existente.
